https://codepen.io/Humza23/pen/MWbzbMY?editors=1100

 1. What is Semantic HTML? Tags with meaning. Defines content
    2. What is HTML used for? It is used to display data
    3. What is an attribute and where do we put it? An attribute is a way to supply data to element
    4. What is the h1 tag used for? How many times should I use it on a page? h1 is the main title. It should only be used once
    5. Name two tags that have required attributes <a> <img src="">
    6. What do we put in the head of our HTML document? 
    7. What is an id? An id is a unique way to select an element
    8. What elements can I add an id to? Any html elements
    9. How many times can I use the same id on a page? 1 time it is unique
    10. What is a class? a class is a way to select a group of elements
    11. What elements can I add a class to? Any html elements
    12. How many times can I use the same class on a page? multiple times
    13. How do I get my link to open in a new tab? <a href="website.com" target="_blank"> New Tab </a>
    14. What is the alt attribute in the image tag used for? alternate text if image cannot be displayed
    15. How do I reference an id? # symbol
    16. What is the difference between a section and a div? section is semantic and thematic grouping. div is only used for grouping and styling
    17. What is CSS used for? cascade styling sheets. used to style a website and make it look "pretty"
    18. How to we select an element? Example - every h2 on the page. h2 { }
    19. What is the difference between a class and an id? - Give me an example of when I might use each one. An id is to select a unique item such as one paragraph. A class may be used to select multiple paragraphs which will have same CSS.
    20. How do we select classes in CSS? Use the period. ".class {}"
    21. How do we select a p element with a single class of “human””? .class p { }
    22. What is a parent child selector? When would this be useful? This is useful to style a specific item in a div or section
    23. How do you select all links within a div with the class of sidebar? .class href{}
    24. What is a pseudo selector? a pseudo selector is used to select html that is being interacted with (hover, unvisited, visited)
    25. What do we use the change the spacing between lines? line-height
    26. What do we use to change the spacing between letters? letter-spacing
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize? text-transform:uppercase, lowercase, capitalize
    28. How do I add a 1px border around my div that is dotted and black? div {border: 1px black dotted}
    29. How do I select everything on the page? body tag
    30. How do I write a comment in CSS? /* */ or cmd + /
    31. How do I find out what file I am in, when I am using the command line? pwd
    32. Using the command line - how do I see a list of files/folders in my current folder? ls
    33. How do I remove a file via the command line? Why do I have to be careful with this? rm file. You can lose your work.
    34. Why should I use version control? Version control allows developers to branch and work on updates without breaking the main app.
    35. How often should I commit to github? Often to not lose work.
    36. What is the command we would use to push our repo up to github?  git push -u origin file-name
    37. Walk me through Lambda's git flow. 
        Fork make own copy. git clone "link" to get locally. Cd into repo. git checkout -b branch-name to branch. Git add . to get changes ready to commit. git commit -m "commit message" to commit the changes. git push -u origin branch-name. Create pull request on github and make sure base is changed to forked version.